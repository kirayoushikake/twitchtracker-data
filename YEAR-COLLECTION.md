# 365 天采集

使用已登录普通 Edge 的 `http://127.0.0.1:9222` 会话，不创建 InPrivate 窗口。

```powershell
.\run-year.ps1
# 或只重试某个主播
.\run-year.ps1 -Channels sayu
# 新目录创建新窗口，旧目录用于续采
.\run-year.ps1 -OutputDir 'output/year-new-batch'
```

本批次数据保存在 `output/year-2026-09-22/`。`window.json` 固定首次运行时的 UTC 起止时刻，窗口为 365 × 24 小时。按开播时间筛选，包含正在直播的场次并明确标记快照。续采沿用同一窗口。

采集器读取历史列表全部分页的记录，而不是只取首屏 20 行；每条记录保存 URL、UTC 开始时间、分钟时长、标题、游戏。逐场原始 JSON 会缓存，运行中断后再次执行会跳过已保存场次。没有 40 场上限。

`listing.json` 是完整历史列表，`collection.json` 是进度及失败清单，`prepared.json` 是校验后的记录。只有时间窗口内每个详情页都读取成功才导出全年 Excel，否则保留失败 URL 并报错。可以重跑补采。

每位主播一个工作簿，首页汇总；之后每场一个 sheet，按时间倒序，内含网页概览、图表数据、页面元数据。时间统一为 UTC+8；UTC 原始列另行保留。分类使用图表游戏标记；单一游戏且无竖线的场次使用 Played Games 中的唯一分类，多游戏但无标记时明确记为缺失。标题不作为游戏分类。

若详情页正常显示概览但网站没有图表容器，保存概览、游戏信息和元数据，在汇总状态及详情图表区域明确标记“网站未提供 CCV 时间序列”，不编造数据点。若存在图表容器但加载失败，仍算采集失败。网站有时在第一个采样时刻同时提供零值起始点与实际采样点，两点均按原顺序保留；关注者数据按相同时间戳的出现顺序匹配。

目前运行环境仍依赖 README 中的 artifact-tool 和 Playwright 库，原始 JSON 不包含浏览器登录 Cookie。不应把个人 Edge 用户目录提交到 GitHub。

详情 sheet 名称包含完整年份，例如 `01_2026-09-19_2345`。序号与汇总页一致。其它批次目录可通过 `run-year.ps1 -OutputDir` 指定；直接运行 Node 阶段命令时使用 `TWITCHTRACKER_BATCH_DIR` 环境变量。新目录按首次运行时间创建 365 天窗口；同一目录重跑沿用窗口和缓存，不自动刷新直播中快照。
